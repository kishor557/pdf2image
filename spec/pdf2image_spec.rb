require 'spec_helper'

describe Pdf2image do
  it 'has a version number' do
    expect(Pdf2image::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
